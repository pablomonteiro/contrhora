<div class="panel-body">
    <div class="container">
        <%= form_for @record, url: record_path(I18n.locale, @record)  do |form| %>
            <%= render 'records/form', f: form %>
        <% end %>
    </div>
</div>